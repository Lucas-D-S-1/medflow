# Dados brutos e pesados
*.dbf
*.dbc
*.parquet
# Escrita atômica do pipeline: o arquivo só vira definitivo ao ser promovido.
# Sem esta regra, uma execução interrompida deixa o .parcial rastreável.
*.parcial
dados/bronze/origem/datasus/
dados/bronze/intermediario/dbf/
dados/bronze/origem/referencias/*.json
dados/bronze/origem/referencias/*.zip
dados/bronze/origem/referencias/*.html
dados/bronze/origem/referencias/geografia/

# Ambiente
.venv/
__pycache__/
.ipynb_checkpoints/
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example

# Oracle Cloud / Autonomous Database — nunca versionar credenciais ou wallets
Wallet_*/
**/Wallet_*/
wallets/
**/wallets/
tnsnames.ora
**/tnsnames.ora
sqlnet.ora
**/sqlnet.ora
cwallet.sso
**/cwallet.sso
ewallet.p12
**/ewallet.p12
ewallet.pem
**/ewallet.pem
keystore.jks
**/keystore.jks
truststore.jks
**/truststore.jks
*.pem
*.key

# ai-memory (gerados)
.claude/
.agents/
.mcp.json
.codex/

# Material de trabalho - nunca versionar
*motor_forecast*
*Motor_Forecast*
*x29*
*do_aaa*

# Backups gerados pelos instaladores do ai-memory
*.bak-*

# Metadados locais do Windows
Thumbs.db

# Material oficial do Challenge e artefatos de outras fases do curso ficam no
# repositório acadêmico fiap-1tscoa; não têm caminho equivalente aqui.
01_pbl_cidade_alfa/entregues/fase_4/conteudo_extraido/4ª_entrega_Omega_Urban_Tech_codigo_python_gera_mapa_html.pdf

# Repositorio independente, com remote proprio
01_pbl_cidade_alfa/pbl-cidade-alfa-transporte/

# arquivos de bloqueio do Office (Word/PowerPoint/Excel)
~$*

# Configuração das ferramentas de IA usadas no desenvolvimento. Continuam no
# disco de quem trabalha aqui, mas não fazem parte do que o projeto publica:
# são instruções para o agente, não para quem avalia ou reusa o MedFlow.
# A coautoria em si permanece registrada nos trailers dos commits.
CLAUDE.md
AGENTS.md
.ai-memory.toml
.ai-memory.toml.bak-*
